<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Website - {{site.title}}</title>
    <meta name="description" content="Retrouvez ici toutes les photos de Charles Cantin qui déchirent !">
    <link rel="stylesheet" href="{{ 'css/style.css' | url }}">
</head>
<body>
    {% include "header.njk" %}
      {{ content | safe }}
    {% include "footer.njk" %}
</body>
</html>